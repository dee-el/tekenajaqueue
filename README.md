## Usage

1.**Run the Program:**

```bash
go run main.go
```

2.**Add a Task via HTTP:**
Send a GET request to `http://localhost:8080/tasks`, it will generate task with random ID and execute time 5 seconds ahead from now.
